# Customer accounts are stored in the "Users" Google Sheet, not in Django's
# SQL database, so there is nothing to register with Django's admin site here.
# Manage customers via /dashboard/users/ instead.
